name: xagg
channels:
  - conda-forge
dependencies:
  - pytest
  - xarray
  - numpy
  - scipy
  - netcdf4
  - pandas
  - geopandas
  - xesmf >= 0.5.2
  - cf_xarray # separately, to ensure latest version
  - pytables